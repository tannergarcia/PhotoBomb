import { Component } from '@angular/core';
import { UserData } from '@app/icons';

@Component({ templateUrl: 'table_display.html' })
export class TableDisplayComponent { 

    display(){
        const amount = UserData;
    }
}